// import { useProcessesContext } from '@/context/processes';
// import { revalidatePath } from 'next/cache';

const ArchivedCount =  () => {

  // const {processes} = useProcessesContext();
  // const countProcessesArchived = processes.filter((process) => process.type === 'archived').length;
  
  return(
    <>
      <p>ARQUIVADOS </p>
    </>
  );
};

export default ArchivedCount;